/*
 * SectionInformasi Messages
 *
 * This contains all the text for the SectionInformasi container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SectionInformasi';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SectionInformasi container!',
  },
});
