// import { selectFormDocumentDomain } from '../selectors';

describe('selectFormDocumentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
