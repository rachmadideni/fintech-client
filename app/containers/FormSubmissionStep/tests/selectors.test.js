// import { selectFormSubmissionStepDomain } from '../selectors';

describe('selectFormSubmissionStepDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
