// import { selectProductSelectionDomain } from '../selectors';

describe('selectProductSelectionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
