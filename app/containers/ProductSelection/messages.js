/*
 * ProductSelection Messages
 *
 * This contains all the text for the ProductSelection container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProductSelection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProductSelection container!',
  },
});
